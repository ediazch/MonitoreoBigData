# Security Report — proyecto-completo
Fecha: 2026-09-18 19:20:42

## Resumen
| Severidad | Cantidad |
|---|---|
| CRITICAL | 2 |
| HIGH | 11 |
| MEDIUM | 16 |
| LOW | 42 |

## Hallazgos

### [CRITICAL] Eval con input externo — SEC-003
- **Archivo**: `skills\security.py` linea 57
- **Descripcion**: eval()/exec() pueden ejecutar codigo arbitrario
- **Recomendacion**: Evitar eval/exec. Usar ast.literal_eval() para datos simples
- **Codigo**: `"descripcion": "eval()/exec() pueden ejecutar codigo arbitrario",`

### [CRITICAL] Eval con input externo — SEC-003
- **Archivo**: `skills\security.py` linea 58
- **Descripcion**: eval()/exec() pueden ejecutar codigo arbitrario
- **Recomendacion**: Evitar eval/exec. Usar ast.literal_eval() para datos simples
- **Codigo**: `"recomendacion": "Evitar eval/exec. Usar ast.literal_eval() para datos simples"`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `analisis_relacion.py` linea 84
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"        ODS  -> {len(hojas_ods)} hojas cargadas: {list(hojas_ods.keys())}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 262
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens antes   : {tokens_antes}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 263
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens despues : {tokens_despues}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 338
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens en observaciones raw : {tokens_raw}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 339
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens en resumenes         : {tokens_comprimidos}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 342
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Ahorro estimado de tokens   : {ahorro}%")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `memory\compresor.py` linea 346
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Algoritmo fallback          : Reglas inteligentes (frecuencia + keywords)")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `skills\headroom.py` linea 267
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens antes  : {r['tokens_antes']:,}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `skills\headroom.py` linea 268
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"  Tokens despues: {r['tokens_despues']:,}")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `skills\headroom.py` linea 270
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"\n  ORIGINAL ({r['tokens_antes']} tokens):")`

### [HIGH] Datos sensibles en logs — SEC-007
- **Archivo**: `skills\headroom.py` linea 272
- **Descripcion**: Datos sensibles pueden aparecer en logs
- **Recomendacion**: Nunca loggear credenciales. Usar logging.debug('[REDACTED]')
- **Codigo**: `print(f"\n  COMPRIMIDO ({r['tokens_despues']} tokens):")`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\compresor.py` linea 94
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\compresor.py` linea 105
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception as e:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\compresor.py` linea 182
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 125
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception as e:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 162
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception as e:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 197
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 210
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 224
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `memory\watcher.py` linea 263
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\headroom.py` linea 52
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\headroom.py` linea 224
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\ralph.py` linea 71
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception as e:`

### [MEDIUM] Debug mode en produccion — SEC-011
- **Archivo**: `skills\security.py` linea 115
- **Descripcion**: debug=True expone informacion sensible del stack
- **Recomendacion**: Usar variables de entorno para controlar debug mode
- **Codigo**: `"descripcion": "debug=True expone informacion sensible del stack",`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\security.py` linea 154
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\security.py` linea 301
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception as e:`

### [MEDIUM] Excepcion generica capturada — SEC-008
- **Archivo**: `skills\task_observer.py` linea 299
- **Descripcion**: Capturar Exception generica oculta errores de seguridad
- **Recomendacion**: Capturar excepciones especificas: except (ValueError, KeyError) as e
- **Codigo**: `except Exception:`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 38
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("1069729185") == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 42
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("'1069729185'") == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 46
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("  1069729185  ") == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 50
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("10697-29185") == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 53
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id('"1069729185"') == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 57
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("") == ""`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 60
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_id("   ") == ""`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 69
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_tipo("1") == "1"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 72
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_tipo("  12  ") == "12"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 75
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert limpiar_tipo("13") == "13"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 86
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 87
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado[0]["tipo_id"] == "1"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 88
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado[0]["num_id"] == "1069729185"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 94
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 95
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado[0]["tipo_id"] == "4"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 100
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 3`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 102
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert "1" in tipos`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 103
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert "13" in tipos`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 106
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert normalizar_filas_csv([]) == []`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 124
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 125
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado[0]["num_id"] == "2000000023"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 131
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 136
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado[0]["hoja"] == "4_TI"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 144
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 3`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 157
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(eliminar_duplicados(registros)) == 2`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 166
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado) == 2`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 169
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert eliminar_duplicados([]) == []`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 193
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado["coincidencias"]) == 2`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 197
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado["solo_en_csv"]) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 198
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["solo_en_csv"][0]["num_id"] == "999"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 202
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert len(resultado["solo_en_ods"]) == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 203
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["solo_en_ods"][0]["num_id"] == "888"`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 207
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert "1" in resultado["resumen_por_tipo"]`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 208
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["resumen_por_tipo"]["1"]["coincidencias"] == 2`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 214
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["resumen_por_tipo"]["1"]["cobertura_pct"] == 100.0`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 218
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["coincidencias"] == []`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 219
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["solo_en_csv"] == []`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 220
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert resultado["solo_en_ods"] == []`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 236
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert stats["total_registros_csv"] == 2`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 237
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert stats["coincidencias"] == 1`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 238
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert stats["cobertura_csv_pct"] == 50.0`

### [LOW] Uso de assert para validacion — SEC-013
- **Archivo**: `tests\test_limpieza.py` linea 248
- **Descripcion**: assert se desactiva con python -O, no usar para validacion de seguridad
- **Recomendacion**: Usar if/raise ValueError en lugar de assert para validaciones criticas
- **Codigo**: `assert stats["cobertura_csv_pct"] == 100.0`

